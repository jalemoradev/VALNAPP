import React from 'react';
import { DismissKeyboard } from './DismissKeyboard';

describe('DismissKeyboard', () => {
  it('should export a valid component', () => {
    expect(DismissKeyboard).toBeDefined();
    expect(typeof DismissKeyboard).toBe('function');
  });
});
